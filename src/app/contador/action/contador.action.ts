import { createAction } from '@ngrx/store';

export const decremente = createAction(
  '[Decremente] Decrementar Numero'
);

export const incremente = createAction(
  '[Incremente] Incrementar Numero'
);

